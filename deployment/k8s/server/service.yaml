apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: vercel
  labels:
    app: server
spec:
  selector:
    app: server
  ports:
    - name: http
      port: 4500
      targetPort: 4500
      protocol: TCP
  type: ClusterIP